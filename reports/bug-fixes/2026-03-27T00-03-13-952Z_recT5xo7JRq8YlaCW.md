# Bug Fix Report: recT5xo7JRq8YlaCW

**Priority:** Low
**Page:** Settings
**Decision:** failed
**Category:** NEEDS_CLAUDE_CODE

## Feedback
Phase 7 Security: The selected program ID (symphonix-selected-program) is stored in localStorage as a plain UUID. While not a direct vulnerability, this value is used to scope API requests and is readable by any script on the page (including third-party analytics scripts from Google and Facebook that are loaded). If an attacker obtains this ID, they can use it with the unauthenticated API endpoints to pull all program data. Recommendation: After fixing API authentication, this becomes lower risk; consider using server-side session state instead.

QA NOTE (3/25/26): NOT FIXED. Program ID still stored in localStorage, readable by third-party scripts.

QA NOTE (2026-03-25): NOT FIXED. Program ID is still stored in localStorage rather than a secure, server-side session.

## Analysis
The selected program ID was stored in `localStorage` under the key `symphonix-selected-program`, making it readable by any JavaScript on the page (including third-party analytics scripts). Moved storage to an httpOnly cookie managed by a server-side API route, so the value is no longer accessible to client-side scripts.

## Fix Result
**Success:** Yes

### Changes
1. **`app/api/selected-program/route.ts`** (new) — Server-side API route with GET and PUT handlers that read/write the program ID as an httpOnly, secure, SameSite=lax cookie.
2. **`app/tools/scheduler/admin/ProgramContext.tsx`** (modified) — Replaced all `localStorage.getItem`/`setItem` calls with `fetch('/api/selected-program')` calls. Added a one-time migration that removes the old localStorage key on first load.


---
*Generated: 2026-03-27T00:03:13.952Z*
