# Bug Fix Report: rec1W2HDYQRsiAAQX

**Priority:** Medium
**Page:** Settings
**Decision:** fixed
**Category:** UNCATEGORIZED

## Feedback
PROBLEM: None of the script tags on the page have Subresource Integrity (SRI) attributes. If the hosting server is compromised, modified scripts would execute without detection.

HOW TO FIX (for Next.js / Turbopack):
Note: SRI is tricky with Next.js because script filenames change on every build. The standard approach is:
1. If you are self-hosting (not using a CDN for your JS), SRI is less critical since the scripts come from your own server. Consider whether this is a priority vs. other items.
2. If you want to proceed, you can use the next-sri package: npm install next-sri, then wrap your next.config.js with it. This auto-generates integrity hashes at build time.
3. Alternatively, add a Content-Security-Policy header with strict script-src directives (you already have a CSP header, which is good). Make sure it includes 'strict-dynamic' or specific hashes for inline scripts.
4. At minimum, for any third-party scripts loaded from external CDNs, manually add integrity="sha384-..." and crossorigin="anonymous" attributes.

WHAT I WILL CHECK: I will look for integrity attributes on script tags. If you decide this is not worth pursuing for self-hosted Next.js assets, let the team know and we can close this as "won't fix" instead.

---
QA NOTE (2026-03-31): NOT FIXED. All 15 script tags on the page still have no integrity attribute. 0 scripts have SRI. Since these are all self-hosted Next.js chunks from tools.artsnwct.org/_next/static/chunks/, this may be acceptable as a "won't fix" if the team decides SRI for self-hosted assets is low priority.

## Analysis
Bug fixed and deployed

## Fix Result
**Success:** Yes

### Changes
- Delegated to Claude Code
- Output: Good. Since scripts are self-hosted (same origin), `crossOrigin` isn't strictly required, but the integrity attributes will still be present.

Here's a summary of the changes:

## Changes Made

**1. `...




---
*Generated: 2026-04-01T05:07:57.520Z*
