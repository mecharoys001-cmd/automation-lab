# Bug Fix Report: recSd7hEZNbd4mnXq

**Priority:** Low
**Page:** Staff/Venues
**Decision:** already_fixed
**Category:** ALREADY_FIXED

## Feedback
PROBLEM: No elements on the page use the loading="lazy" attribute. Below-fold content like staff cards and availability grids renders eagerly.

HOW TO FIX:
1. For any <img> tags that appear below the fold, add loading="lazy". In Next.js, if you use the <Image> component, set the loading="lazy" prop (it is the default for images without priority).
2. For below-fold sections like individual staff cards on the Staff/Venues page, wrap them in a lazy-loading container. You can use the native Intersection Observer API or a library like react-intersection-observer:
   import { useInView } from 'react-intersection-observer';
   function StaffCard({ staff }) {
     const { ref, inView } = useInView({ triggerOnce: true, rootMargin: '200px' });
     return <div ref={ref}>{inView ? <FullStaffCard staff={staff} /> : <StaffCardSkeleton />}</div>;
   }
3. This is most impactful on the Staff/Venues page (cards below the first 3-4) and the School Calendar page (months below the current viewport).

WHAT I WILL CHECK: I will search for loading="lazy" attributes on the page. At minimum, one or more elements should have this attribute.

## Analysis
Bug was already fixed in previous work



---
*Generated: 2026-03-30T20:00:09.575Z*
